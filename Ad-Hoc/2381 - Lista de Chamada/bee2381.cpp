// Beecrowd 2381 - Lista de Chamada
#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // Lendo a quantidade de alunos (N) e a posição da chamada (P)
    int N, P;
    cin >> N >> P;
    // Percorrendo e ordenando os alunos pela ordem alfabética 
    for (int i = 0; i < N; i++) {
        // Lê os nomes dos alunos
        string name; 
        cin >> name;
    }

    return 0;
}
